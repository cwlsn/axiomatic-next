import { colors, spacing, typography } from '@axiomatic/tokens';

// https://github.com/primer/primitives/blob/master/theme/index.ts
const theme = {
    colors,
    space: spacing,
    fontSizes: typography.fontSizes,
    lineHeights: typography.lineHeights
};

export default theme;
